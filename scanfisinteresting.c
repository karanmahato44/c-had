#include<stdio.h>
int main(void) {
    int a, b, sum, y, x;
    printf("Enter two nums: ");
    x=scanf("%d%d",&a,&b);
    printf("sum is %d");
    y=printf("\nx is %d",x);
    printf("\ny is %d");
    return 0;
}